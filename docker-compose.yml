version: '3'

services:
  openvpn:
    build: .
    container_name: openvpn
    volumes:
      - ./openvpn-data:/etc/openvpn
    ports:
      - "1194:1194/udp"
    cap_add:
      - NET_ADMIN
    privileged: true
    restart: unless-stopped 